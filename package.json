{
  "name": "ao3-hub",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "shared",
    "server",
    "web"
  ],
  "scripts": {
    "dev": "concurrently -n server,web -c blue,magenta \"bun run --cwd server dev\" \"bun run --cwd web dev\"",
    "dev:server": "bun run --cwd server dev",
    "dev:web": "bun run --cwd web dev",
    "typecheck": "bun run --cwd server typecheck && bun run --cwd web typecheck",
    "build:web": "bun run --cwd web build",
    "build": "bun run --cwd server build",
    "ci": "bun run typecheck && bun run build",
    "ota:manifest": "bun run server/scripts/release-manifest.ts",
    "start": "bun run --cwd server start"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "typescript": "^5.6.3"
  }
}
